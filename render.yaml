services:
  - type: web
    name: gabfoot-web
    runtime: python
    plan: free
    region: frankfurt
    rootDir: .
    buildCommand: pip install -r requirements.txt
    startCommand: python web_app.py
    autoDeploy: true
